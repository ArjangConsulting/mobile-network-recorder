interface:
  display_name: "APITrace Integration"
  short_description: "Add the APITrace mobile network recorder to iOS and Android apps"
  default_prompt: "Integrate APITrace into this app's real networking path, keep release behavior safe, configure redaction deliberately, and add a developer-only export flow."
